// Placeholder chart component; replace with real chart library later (e.g., Chart.js, Recharts, etc.)
export default function ChartPlaceholder({ label = 'Chart' }) {
  return (
    <div className="chart-placeholder">
      <p>{label} (coming soon)</p>
    </div>
  );
}
